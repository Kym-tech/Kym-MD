services:
  - type: web
    name: KYM-MD
    env: docker
    region: Frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm run toxictech"
    envVars:
      - key: PORT
        value: "3000"
